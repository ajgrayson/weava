.panel.panel-default
  .panel-heading
    %h3.panel-title New Project
  .panel-body
    = form_for :project, :url => projects_path do |f|
      .form-group
        = f.label :name
        = f.text_field :name, :class => 'form-control'
      .form-group
        = f.submit 'Save', :class => 'btn btn-primary'
        = link_to 'Cancel', projects_path, :class => 'btn'