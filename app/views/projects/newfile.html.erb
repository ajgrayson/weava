<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">New File</h3>
  </div>
  <div class="panel-body">
    
  <form method="post" action="/projects/<%= @project.id.to_s %>/createfile">

  <%= form_tag "/projects/" + @project.id.to_s + "/createfile" do %>

    <div class="form-group">
      <%= label_tag "name" %>
      <%= text_field_tag "name", "", :class => "form-control" %>
    </div>

    <div class="form-group">
      <%= label_tag "content" %>
      <%= text_area_tag "content", "", :class => "form-control" %>
    </div>

    <div class="form-group">
        <%= submit_tag "Save", :class => 'btn btn-primary' %>
        <%= link_to "Cancel", project_path(@project), :class => 'btn' %>
    </div>

    <% end %>

  </div>

</div>